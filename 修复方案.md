# 中国象棋「多模式」修复方案

> 项目路径：`workCodes/中国象棋/index.html`（单文件，纯前端）
> 诊断日期：2026-09-04
> 诊断方式：本地 HTTP 服务 + 无头浏览器实测复现

---

## 一、问题总览

| 级别 | 编号 | 问题 | 影响 | 修复难度 |
|------|------|------|------|----------|
| 🔴 P0 | BUG-1 | 首屏棋盘空白，未触发首帧绘制 | 打开页面像死机 | 1 行 |
| 🔴 P0 | BUG-2 | AI 思考必崩：调用不存在的方法 `isCheckmate` | 走一步后游戏死锁在「思考中...」 | 6 行 |
| 🟡 P1 | BUG-3 | 内臣局被吃方判断反了 | 内臣继位触发错误一方 | 1 行 |
| 🟡 P1 | BUG-4 | AI 异步回调与重开/切模式竞态 | 旧 AI 污染新棋局 | 4 行 |
| 🟡 P1 | BUG-5 | 悔棋逻辑边界不严谨 | 历史不足时行为不一致 | 3 行 |
| 🟢 P2 | BUG-6 | Google Fonts 外网依赖 | 无网/弱网首屏字体卡顿 | 3 行 |
| 🟢 P2 | BUG-7 | favicon 404 | 控制台报错噪音 | 1 行 |
| 🟢 P2 | BUG-8 | ban 局死代码 `filter(p=>!game.banBlack)` | 逻辑误导，恒为真 | 1 行 |
| 🟢 P2 | BUG-9 | 心腹局 render 冗余恒等判断 | 无实际错误，代码冗余 | 可选 |

---

## 二、详细诊断与修复

### BUG-1 首屏棋盘空白

**定位**：文件末尾第 532 行

```js
updateStatus('红方走棋',SIDE_RED);
```

初始化流程只更新了状态文字，**从未调用 `renderer.draw()`**，因此 canvas 首帧为空（实测非透明像素 = 0）。只有点击「重新开局」或切换模式触发 `restartGame()` 才会绘制。

**修复**：末尾追加一行

```js
updateStatus('红方走棋',SIDE_RED);
renderer.draw(); // ★ 初始化后立即绘制首帧
```

---

### BUG-2 AI 思考必崩（核心致命 bug）

**定位**：`ChessAI.search()` 约第 180 行

```js
const c=this.board.clone();c.move(m.fr,m.fc,m.tr,m.tc);if(c.isCheckmate()){best=m;bs=1e5;break;}
```

`c` 是 `ChessBoard` 实例，但 `ChessBoard` 类（第 113-172 行）**没有 `isCheckmate` 方法**——该方法只存在于 `MODES.classic` 等模式对象上。AI 一思考就抛 `TypeError: c.isCheckmate is not a function`，`best` 保持 `null`，状态永远卡在「思考中...」，对局死锁。

**修复**：在 `ChessBoard` 类内 `evaluate()` 方法之后（第 171 行后）新增：

```js
isCheckmate(){
  if(this.gameOver)return true;
  for(const p of this.getPieces(this.turn)){
    if(this.getLegalMoves(p.row,p.col).length>0)return false;
  }
  return true;
}
```

新增后 `search()` 中 `c.isCheckmate()` 即可正常工作：AI 走完一手后若对方无棋可走，直接判杀。

> 可选：将 `MODES.classic.isCheckmate` 等模式方法改为复用 `board.isCheckmate()`，消除重复实现：
> ```js
> isCheckmate(board,game){ return board.isCheckmate(); }
> ```

---

### BUG-3 内臣局被吃方判断反了

**定位**：`MODES.inner.onMove` 第 340 行

```js
const side=1-board.turn; // 被吃的是哪一方  ← 逻辑反了
```

以红方吃黑方将为例：`move()` 执行后 `board.turn` 已切换为 `SIDE_BLACK(1)`，此时 `1-board.turn = 0 = SIDE_RED`，代码会去触发**红方**内臣继位——但被吃的是**黑方**将，应该触发黑方内臣。

**修复**：直接用被吃棋子的所属方

```js
const side=captured.side; // ★ 被吃棋子的 side 即被吃方
```

---

### BUG-4 AI 异步回调竞态

**定位**：`onBoardClick` 第 476 行 AI 走棋段

```js
board.aiThinking=true;updateStatus('思考中...',SIDE_BLACK);
setTimeout(()=>{ const depth=...; const ai=new ChessAI(board,depth); const move=ai.search(...); ... },100);
```

AI 在 `setTimeout` 里操作**同一个 `board` 引用**。若 AI 思考期间用户点了「重新开局」或切换模式（`restartGame()` 会 `board.reset()`），旧 AI 回调仍会执行，在新棋盘上凭空走一步，造成棋局被污染。

**修复**：引入代次（epoch）令牌，旧回调直接丢弃。

1. `game` 对象初始化处（第 448 行）加字段：
```js
const game={selected:null,legalMoves:[],phase:'play',currentMode:'classic',lastMove:null,epoch:0};
```

2. `restartGame()`（第 485 行）内递增代次：
```js
function restartGame(){
  hideModal();board.reset();
  game.selected=null;game.legalMoves=[];game.lastMove=null;board.aiThinking=false;
  game.epoch++; // ★ 使旧 AI 回调失效
  ...
}
```

3. AI 走棋段记录并校验代次：
```js
board.aiThinking=true;updateStatus('思考中...',SIDE_BLACK);
const myEpoch=game.epoch; // ★ 记录当前代次
setTimeout(()=>{
  if(myEpoch!==game.epoch){board.aiThinking=false;return;} // ★ 已重开/切模式，丢弃
  ...
},100);
```

---

### BUG-5 悔棋逻辑边界

**定位**：第 513-517 行

```js
board.undo();if(board.moveHistory.length>0)board.undo();
```

「悔两步」的意图是回退到红方回合。但历史不足 2 条（如 AI 超时未走棋、仅玩家走了 1 步）时只悔 1 步，且无条件回到红方回合。

**修复**：悔到红方回合为止，并保护边界

```js
board.undo();
if(board.moveHistory.length>0&&board.turn!==SIDE_RED)board.undo();
```

---

### BUG-6 Google Fonts 外网依赖

**定位**：第 8 行

```css
@import url('https://fonts.googleapis.com/css2?family=Noto+Serif+SC:wght@400;700;900&family=Ma+Shan+Zheng&display=swap');
```

无外网环境会阻塞/回退，且有隐私顾虑。

**修复**：删除该行，字体栈改系统字体：

```css
/* CSS 中 */
body{font-family:'Noto Serif SC','Songti SC','SimSun','Microsoft YaHei',serif;...}
.panel-header h1{font-family:'Ma Shan Zheng','STKaiti','KaiTi',cursive;...}

/* JS 中两处 ctx.font */
ctx.font='18px "Ma Shan Zheng","STKaiti","KaiTi",cursive';
ctx.font='bold 20px "Noto Serif SC","Songti SC","SimSun",serif';
```

---

### BUG-7 favicon 404

**定位**：`<head>` 区。

**修复**：加一行消除 404：

```html
<link rel="icon" href="data:,">
```

---

### BUG-8 ban 局死代码

**定位**：第 231 行

```js
const pp=board.getPieces(SIDE_BLACK).filter(p=>!game.banBlack);
```

filter 执行时 `game.banBlack` 必为 `null`，`!null` 恒为 `true`，过滤无效（行为碰巧等效，纯误导）。

**修复**：

```js
const pp=board.getPieces(SIDE_BLACK);
```

---

### BUG-9（可选）心腹局 render 冗余判断

**定位**：第 309 行

```js
if(hp&&hp.type===h.type&&hp.side===board.board[h.row][h.col].side)
```

`board.board[h.row][h.col]` 就是 `hp`，最后一段恒真，属冗余。可简化为：

```js
if(hp&&hp.type===h.type&&hp.side===board.turn?0:1) // 或直接去掉末尾判断
```

---

## 三、待确认的设计问题（建议跟进）

**内臣继位后的走法**：`MODES.inner.onMove` 继位时 `{type:ip.type,side:side,innerKing:true}` 保留原棋子类型。若内臣是兵，`getRawMoves` 的 PAWN 分支有 `innerKing` 额外攻击距离加成；但若内臣是车/马/炮，继位后仍按原棋子走法移动，**不像"将"那样受九宫格限制，也无法被正常将死判定外的规则约束**。建议确认设计意图：

- 方案 A：内臣继位后 `type` 改为 `KING`（走九宫格将步），`innerKing` 标记用于显示 👑；
- 方案 B：保持现状（原类型走法 + 兵额外加成），但需在规则说明中写清楚。

> ✅ **已确认（2026-09-04）**：采用方案 B——内臣继位后仍按原棋子走法移动，不受九宫格限制；若内臣为兵，保留 `innerKing` 额外 1 格攻击距离加成。代码无需改动。

---

## 四、修复顺序与验收清单

### 建议修复顺序

1. BUG-2（加 `isCheckmate` 方法）→ 2. BUG-1（首帧绘制）→ 3. BUG-3 → 4. BUG-4 → 5. BUG-5 → 6. BUG-6/7/8/9

### 验收清单（每项都要实测）

| # | 验收项 | 操作 | 预期 |
|---|--------|------|------|
| 1 | 首屏渲染 | 直接打开 index.html | 棋盘、棋子、楚河汉界立即可见 |
| 2 | 经典局走棋 | 走一步兵 | AI 正常思考并在数秒内回手，状态恢复「红方走棋」 |
| 3 | AI 将杀 | 高手难度下让 AI 将死红方 | 弹出「黑方胜」弹窗，可再来一局 |
| 4 | 悔棋 | 走 2 步后点悔棋 | 回到红方回合，棋盘同步回退 |
| 5 | ban 局 | 选 ban 局，红方禁 1 子 | AI 自动禁黑子，弹窗后正常开局 |
| 6 | 心腹局 | 选心腹局，红方选心腹 | 走棋、心腹高亮、将死时心腹牺牲提示正常 |
| 7 | 内臣局 | 选内臣局，红方选内臣 | 红方内臣可见（蓝标），对局正常 |
| 8 | 内臣继位 | 吃掉黑方将 | 黑方内臣（若有）继位，弹出「内臣继位」，👑 标记显示 |
| 9 | 重开竞态 | AI 思考时点「重新开局」 | 新棋局不被旧 AI 污染，无异常走子 |
| 10 | 无网环境 | 断网打开页面 | 页面秒开，字体回退到系统字体，无卡顿 |

---

## 五、附：环境备注

- 修复后用任意静态服务器打开即可（本项目无构建步骤，纯单文件 HTML + Canvas）。
- 若需快速自测，可在项目目录执行 `python -m http.server 8765` 后访问 `http://localhost:8765/index.html`。
